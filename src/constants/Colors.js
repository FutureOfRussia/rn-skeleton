export default {
  // Main colors
  white(opacity = 1) {
    return `rgba(255, 255, 255, ${opacity})`
  },
  black(opacity = 1) {
    return `rgba(0, 0, 0, ${opacity})`
  },
  //

  // Gray
  lightGray: '#F5F4F5',
  mediumGray: '#C6C6C6',
  gray: '#808080',
  //

  // Supporting colors
  disabled: '#F5F4F5',
  transparent: 'transparent',
  //
}
